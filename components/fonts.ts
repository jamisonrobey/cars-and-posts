import { Fugaz_One } from "next/font/google";
import { Open_Sans } from "next/font/google";
export const logoFont = Fugaz_One({
  weight: "400",
  subsets: ["latin"],
});

export const sans = Open_Sans({
  weight: ["400"],
  subsets: ["latin"],
});
